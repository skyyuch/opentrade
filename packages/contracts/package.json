{
  "name": "@opentrade/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Solidity smart contracts (Foundry). Source of truth for on-chain logic and exported ABIs consumed by the frontend.",
  "type": "module",
  "files": [
    "src",
    "abi",
    "README.md"
  ],
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:unit": "forge test",
    "test:ci": "forge test --no-match-test testFork",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "lint": "solhint --config .solhint.json --noPrompt 'test/**/*.sol'",
    "typecheck": "echo '@opentrade/contracts: no TypeScript bindings yet (forge build covers solc compilation)' && exit 0",
    "clean": "forge clean && rm -rf .turbo"
  }
}
