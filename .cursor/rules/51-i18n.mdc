---
description: i18n 三語架構（zh-Hant / zh-Hans / en）規範
globs: ["apps/**/*.{ts,tsx}", "packages/ui/**/*.{ts,tsx}"]
alwaysApply: false
---

# i18n Standards

詳見 ADR-0003。

## 支援語言

| Locale Code | 名稱 | 用途 |
|---|---|---|
| `zh-Hant` | 繁體中文 | **預設**，HK 主要市場 |
| `zh-Hans` | 簡體中文 | 中港跨境用戶、大陸 KOL |
| `en` | English | 國際投資者、媒體、政府基金 |

---

## 前端工具：next-intl

### 訊息檔位置

```
apps/web/messages/
├── zh-Hant.json    # 預設
├── zh-Hans.json
└── en.json

apps/console/messages/
├── zh-Hant.json
├── zh-Hans.json
└── en.json
```

### 訊息檔結構

按 namespace 分組：

```json
{
  "common": {
    "submit": "提交",
    "cancel": "取消",
    "loading": "載入中..."
  },
  "reviews": {
    "form": {
      "title": "撰寫評論",
      "bodyPlaceholder": "分享你的真實體驗..."
    },
    "errors": {
      "tooShort": "評論至少 10 字",
      "tooLong": "評論最多 2000 字"
    }
  },
  "brokers": { ... }
}
```

### 使用方式

```tsx
// Server Component
import { getTranslations } from 'next-intl/server';

export default async function Page() {
  const t = await getTranslations('reviews.form');
  return <h1>{t('title')}</h1>;
}

// Client Component
'use client';
import { useTranslations } from 'next-intl';

export function ReviewForm() {
  const t = useTranslations('reviews.form');
  return <input placeholder={t('bodyPlaceholder')} />;
}
```

---

## Routing

URL 必須含 locale：

- `/zh-Hant/brokers` ✅
- `/brokers` ❌（沒 locale prefix）

`middleware.ts` 處理：
- 未含 locale → 從 Accept-Language → cookie → 預設（zh-Hant）
- 切換語言時更新 cookie

### `localePrefix` 策略選擇

每個 app 在 `src/i18n/routing.ts` 必須明確選 `localePrefix`：

| 策略 | URL 樣式 | 適用場景 |
|---|---|---|
| `'always'` | `/zh-Hant/foo`、`/en/foo` 全帶前綴 | console 或任何**不需 SEO** 的後台 app |
| `'as-needed'` | 預設 locale `/foo`、其他 `/en/foo` | apps/web 等需要 SEO-clean 預設 URL 的 public app |

#### ⚠️ App Router `[locale]` 動態段衝突

`'as-needed'` 在 Next.js App Router 有一個非顯而易見的陷阱：

- App Router 用 `app/[locale]/...` 結構匹配 URL
- 預設 locale 不帶前綴時（如 `/admin/users`），App Router 會把 `admin` 當作 `[locale]` 的值嘗試匹配
- `admin` 不在合法 locale 清單裡 → `hasLocale()` 守門失敗 → 404

→ **凡是有 top-level segment（`admin`、`broker`、`api`、`brokers` 等）可能與 locale 字串混淆的 app，必須用 `'always'`**，避免 client-side navigation 觸發 404 或誤匹配。

當前選擇：

- `apps/web` → `'as-needed'`（SEO-clean URL，無 admin/broker 等保留段）
- `apps/console` → `'always'`（admin / broker top-level，加上 robots disallow-all 不影響 SEO）

### Navigation 強制走 `i18n/navigation`

每個 next-intl app 都會在 `src/i18n/navigation.ts` 用 `createNavigation(routing)` 匯出 locale-aware 的 `Link` / `useRouter` / `usePathname` / `redirect`。所有 client 與 server component 必須從這裡匯入，**不可**從 `next/link` 或 `next/navigation` 直接匯入。

```tsx
// ❌ 不可
import Link from 'next/link';
import { useRouter } from 'next/navigation';

<Link href={`/${locale}/admin/users`}>...</Link>
router.replace(`/${locale}`);

// ✅ 應該
import { Link, useRouter } from '../../i18n/navigation';

<Link href="/admin/users">...</Link>  // locale 由 next-intl 自動處理
router.replace('/');
```

理由：
- 從 `next/*` 匯入的 `Link` 不認識 next-intl 的 `localePrefix` 策略，會把 href 原封不動丟給 router → 跟 middleware 與 App Router 三方打架
- 硬編碼 `/${locale}/...` 前綴等於繞過 next-intl，當你切換 `localePrefix` 策略（如本次 `as-needed → always`）時會全面壞掉
- next-intl 的 `Link` 會根據 `routing.localePrefix` 自動加 / 不加前綴

---

## 後端錯誤訊息

API 不直接回傳人類可讀文字，而是 `error_code`：

```json
{
  "error": {
    "code": "REVIEW_TOO_SHORT",
    "message": "Review body too short" // 給開發者看，不是給使用者
  }
}
```

前端依 `code` 在 i18n 訊息檔翻譯：

```json
// zh-Hant.json
{
  "errors": {
    "REVIEW_TOO_SHORT": "評論至少 10 字"
  }
}
```

---

## DB 多語欄位

OpenTrade 有三種模式，依資料來源與彈性需求擇一：

### 模式 A：平行欄位（**當前 Broker 表使用**）

來源資料（如 SFC 公開 register）天生只有英 / 繁中兩語，且不會再加新語言時。
OpenTrade 額外把 SFC 繁中經 OpenCC 轉成簡中存第三列，避免簡中讀者全程看到繁體（per ADR-0026）：

```prisma
model Broker {
  legalName         String  // English（SFC `nameEng`）— 對應 locale = 'en'
  displayName       String  // Traditional Chinese（SFC `nameChi`，無中文時 fallback 英文）— 對應 locale = 'zh-Hant'
  displayNameZhHans String? // Simplified Chinese（OpenCC t→cn from displayName）— 對應 locale = 'zh-Hans'
}
```

⚠️ **這是專案曾出過 4+ 次的回歸點**。新進 agent 改 API payload / UI component 時，
若直接使用 `b.displayName` 或 `{slug}`，等於把英文 locale 使用者鎖在中文（或反向），
或把簡中讀者鎖在繁體。本節是**強制紅線**，不是可選實作。

`displayNameZhHans` 為什麼 nullable：
1. 來自 OpenCC 自動轉換，極少數 unicode codepoint 可能轉換失敗（best-effort backfill）
2. Admin 將來可能想覆寫個別 broker 的轉換結果（部分 HK 繁體俗語自動轉簡中後語感不準）
3. Nullable 讓 helper 自然 fallback 到 `displayName`，比寫一個語感錯誤的值好

#### A1. Canonical helper（強制使用，不要再寫第二份）

```ts
// packages/shared/src/i18n/brokerName.ts
import { localizedBrokerName } from '@opentrade/shared';

// 規則（三路 fallback chain）：
//   - en        → legalName ?? displayName ?? slug
//   - zh-Hans   → displayNameZhHans ?? displayName ?? legalName ?? slug
//   - zh-Hant   → displayName ?? legalName ?? slug
//   - 其他       → displayName ?? legalName ?? slug
//
// 任何顯示 broker / KOL / 其他「平行欄位」實體名稱的 UI 都必須走它，
// 包括：表格、card、tooltip、avatar fallback initials、頁面 title、breadcrumb、
// 通知文案、email 模板。
```

新 entity（如未來 KOL nickname / BrokerLicense series / Dispute case title）
若採用 parallel columns，必須在這裡擴充 helper（`localizedKolName`、
`localizedLicenseSeries`、…），**禁止**在 component 自己寫 ternary。

#### A2. API contract — 永遠回傳三列

任何 API endpoint 在 payload 含「平行欄位實體」的引用（broker、kol、…）時，
**必須**同時回傳 `displayName` + `displayNameZhHans` + `legalName`（broker），
或對應的多列名稱。**禁止**只回 slug、只回 displayName、或只回兩列。

```ts
// ❌ 禁止 — 只 ship slug，前端被迫 render `hsbc-broking-securities-hong-kong-limited`
{ brokerSlug: 'hsbc-...', approvedAt: '...' }

// ❌ 禁止 — 只 ship displayName，英文 / 簡中 locale 永遠顯示繁體
{ broker: { slug: 'hsbc-...', displayName: '匯豐證券' } }

// ❌ 禁止 — 漏 displayNameZhHans，簡中讀者永遠回到繁體 fallback
{ broker: { slug: 'hsbc-...', displayName: '匯豐證券', legalName: 'HSBC ...' } }

// ✅ 強制 — 三列都帶（displayNameZhHans 可為 null，但欄位必須存在）
{
  broker: {
    slug: 'hsbc-...',
    displayName: '匯豐證券',         // zh-Hant
    displayNameZhHans: '汇丰证券',   // zh-Hans（可為 null）
    legalName: 'HSBC ...'            // en
  }
}
```

當 entity 沒有 FK relation 可 `include`（例如 `UserVerifiedBroker` 用 slug 做 key），
用 `apps/api/src/shared/brokerHydration.ts#hydrateBrokerNames(slugs, tenantId)`
做一次性 batched 查表後展平到 payload。Helper 內部已 select 三列，呼叫端只要
把 `displayName` / `displayNameZhHans` / `legalName` 一起 spread 進 payload 即可。

#### A3. UI component — 永遠 pipe 進 helper

```tsx
// ❌ 禁止 — 直接 render 欄位
<span>{broker.displayName}</span>
<span>{broker.displayNameZhHans}</span>
<span>{broker.legalName}</span>
<span className="font-mono">{b.brokerSlug}</span>

// ❌ 禁止 — 在 component 內 inline ternary（會跟 helper 不同步）
<span>{locale === 'en' ? broker.legalName : broker.displayName}</span>
<span>{locale === 'zh-Hans' ? broker.displayNameZhHans ?? broker.displayName : broker.displayName}</span>

// ✅ 強制 — 走 canonical helper
import { localizedBrokerName } from '@opentrade/shared';
import { useLocale } from 'next-intl';

const locale = useLocale();
<span>{localizedBrokerName(broker, locale)}</span>
```

如果是 server component，從 `params.locale` 拿 locale 字串。

當 component 透過 props 接收的 broker 是 hand-rolled 型別而非完整 API client 型別，
**必須**在 props type 列入三列（`displayName` / `displayNameZhHans: string | null` /
`legalName: string | null`），不可只列兩列硬塞 helper — TS 會通過，但 zh-Hans
讀者會永遠 fallback 到繁體。

#### A4. 兩列都要顯示的特例

少數場景需要同時顯示中英名（例如 SFC license record、broker detail header
雙行 hierarchy），這時 helper 仍負責「主要那行的選擇」：

```tsx
const primary = localizedBrokerName(broker, locale);
// secondary 應呈現「另一面語言」：
//   - zh-* primary → secondary 顯示 legalName（英文）
//   - en primary   → secondary 顯示 displayName（繁中，因為 SFC 原始來源是繁中）
const secondary = locale === 'en' ? broker.displayName : broker.legalName;
// 只有 secondary !== primary 才 render 第二行（避免重複）
```

不要在 zh-Hant / zh-Hans 之間互當 secondary —— 簡繁互譯不是另一面身分證明，
只是同語言的轉寫。

#### A5. Slug 的容身之處

`slug` 永遠是 routing key，**不是** user-facing 文案。允許出現的位置：

- `<a href={`/brokers/${slug}`}>`、`router.push('/brokers/' + slug)` 等 URL
- `title` / `aria-label` 屬性的次要除錯標識
- 後台 detail 卡片的小灰字 `font-mono` caption（給管理員追溯用，不是表頭）

**禁止**：表格 cell 主內容、card headline、tooltip 主文、modal title、breadcrumb、
通知文案。

未來若擴增第四語（如日文）或希望去除歧義，應 schema 改名為 `nameEn` / `nameZhHant` /
`nameZhHans` / `nameJa` 並寫 ADR；helper 同步擴成 N 路 fallback 即可，**API
contract 與 component 用法不變**（一定要記得連同新欄位 ship 到所有 endpoint
與 client type）。

### 模式 B：JSON 多語欄位

UGC 或可擴增語言的 metadata（如 KOL 自我介紹）：

```prisma
model KolProfile {
  id   String @id
  bio  Json   // { "zh-Hant": "...", "zh-Hans": "...", "en": "..." }
}
```

前端：`bio[locale] ?? bio['zh-Hant']`（永遠 fallback 預設 locale）。

### 模式 C：獨立翻譯表

當翻譯由背景任務動態產生（如評論用 DeepL 翻），用模式 C：

```prisma
model ReviewTranslation {
  reviewId String
  locale   String  // 'zh-Hant' | 'zh-Hans' | 'en'
  body     String

  @@id([reviewId, locale])
}
```

當前 `ReviewTranslation` 採此模式（per ADR-0023）。

---

## 用戶生成內容（評論）翻譯

> ⚠️ **Phase 1 不做自動翻譯**（per [ADR-0027](mdc:docs/decisions/0027-deprecate-ugc-translation.md)
> superseding ADR-0023）。Review 以**作者原文**呈現，不在 submit 時呼叫
> DeepL / OpenAI / Google Translate 任何翻譯服務。

### 當前流程

1. 用戶寫評論，前端送 `sourceLocale: 'zh-Hant' | 'zh-Hans' | 'en'`
   （從當前 next-intl locale 自動帶；後端 fallback 解析 `Accept-Language`）
2. `POST /v1/reviews` zod 驗 `sourceLocale` 必填三選一
3. `SubmitReviewUseCase` 把 `sourceLocale` 一併寫進 `Review.sourceLocale`
4. IPFS pin + on-chain anchor 流程不變（per ADR-0019）
5. `GET /v1/reviews/broker/:slug` **永遠回原文** `Review.title` + `Review.body`，
   **不**讀 `review_translations` 表
6. Frontend `ReviewCard` 在 review header 顯示 `sourceLocale` badge
   （「繁體中文」/「简体中文」/「English」）
7. Review list 上方加一行 disclaimer：「評論以作者原文呈現，不提供自動翻譯」

### 為什麼不翻譯（per ADR-0027）

- 與「公平 / 透明 / 不可篡改 / 平台不介入內容」紅線一致
- HK 金融用語（孖展、補倉、窩輪、結算等）DeepL 易誤譯，反失可信度
- 業界 UGC 平台（GitHub / Reddit / Twitter / Glassdoor）皆不自動翻譯
- 同源 CJK 跨地讀者基本可被動讀繁中（zh-Hans ↔ zh-Hant 互讀成本低）

### 未來路徑（D7 預留）

`DeepLTranslationService` / `ReviewTranslation` 表 / `DEEPL_API_KEY` env 全部
**保留**為 dormant，待未來「reader 點按鈕 → 一次性翻譯 → cache 回 table」的
on-demand 模式落地（需另寫 successor ADR）。

### 嚴禁

- ❌ 不可在 review submit path 自動呼叫翻譯服務（per ADR-0027 D1）
- ❌ 不可從 `review_translations` 讀資料 serve API（per ADR-0027 D5）
- ❌ 不可移除 `Review.sourceLocale` / `ReviewTranslation` / `DeepLTranslationService`
  （per ADR-0027 D3 + D5 保留 dormant）
- ❌ 不可加新 translation provider env（DeepL 已 deprecated，新 provider 需先寫 ADR）

---

## 鏈上資料

- 鏈上**只存原文 hash**
- 翻譯不上鏈（會漂移）
- 顯示時 frontend 根據原文 hash 查 DB 拿對應翻譯

---

## 文化敏感事項

### 繁體中文

- HK 用詞與 TW 不同（質素 vs 品質、信用咭 vs 信用卡）
- 建議由 HK 在地團隊 review
- 字型：思源宋體 / 思源黑體（支援港字）

### 簡體中文

- 注意敏感字詞（金融類用詞符合大陸規範）
- 不可用 TW 慣用語

### 英文

- American English 為基準
- 金融術語符合國際慣例
- 縮寫第一次出現要全名（SFC = Securities and Futures Commission）

---

## 數字 / 日期 / 貨幣

用 `Intl` API：

```ts
// 數字
new Intl.NumberFormat('zh-Hant').format(12345.67)
// "12,345.67"

// 日期
new Intl.DateTimeFormat('zh-Hant', {
  year: 'numeric', month: 'long', day: 'numeric'
}).format(new Date())
// "2026年5月17日"

// 貨幣
new Intl.NumberFormat('zh-Hant', {
  style: 'currency',
  currency: 'HKD',
}).format(100)
// "HK$100.00"
```

---

## 嚴禁

- ❌ Hardcode 任何使用者可見文字
- ❌ 在 component 用 `if (locale === 'zh-Hant')` 顯示不同內容（用 i18n 訊息檔）
- ❌ 在後端回傳人類可讀文字（回 error_code）
- ❌ 把翻譯結果上鏈
- ❌ 在 console 不做 i18n（即使是 admin 用，也要遵守規範）
- ❌ 漏譯欄位（CI 必須檢查 zh-Hant / zh-Hans / en 同步）
- ❌ DB 平行欄位（如 `legalName` / `displayName` / `displayNameZhHans`）在 UI 直接顯示不轉 locale（必須走 `localizedBrokerName(b, locale)` from `@opentrade/shared`，per「DB 多語欄位 模式 A」）
- ❌ API endpoint 在 payload 引用 broker / 其他平行欄位實體時只回 slug、只回 `displayName`、或只回兩列（必須**三列**一起回 — `displayName` + `displayNameZhHans` + `legalName`，per「模式 A · A2」）
- ❌ Component 在 props 型別只列兩列（`displayName` / `legalName`）就把物件丟給 `localizedBrokerName` — zh-Hans 讀者會永遠 fallback 到繁體（per「模式 A · A3」末段）
- ❌ Component 內 inline `locale === 'en' ? ... : ...`，繞過 canonical helper（會跟 helper 不同步，未來擴語言時所有 inline 版都要 grep 改一遍）
- ❌ 把 slug 當 user-facing 文案 render（slug 是 routing key，per「模式 A · A5」）
- ❌ Avatar 縮寫 / fallback initials 直接吃 `displayName.charAt(0)`（locale 切到 `en` 時會變成中文首字）
- ❌ 從 `next/link` 或 `next/navigation` 匯入 `Link` / `useRouter`（永遠走 `src/i18n/navigation`）
- ❌ 在 href 硬編碼 `/${locale}/...` 前綴（next-intl `Link` 會自動處理）
- ❌ 在有保留 top-level segment（admin / broker 等）的 app 用 `localePrefix: 'as-needed'`（會與 App Router `[locale]` 動態段衝突造成 404）
