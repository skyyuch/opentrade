---
description: PostgreSQL + Prisma ORM 規範
globs: ["packages/db/**/*", "apps/api/**/*"]
alwaysApply: false
---

# Database & Prisma Standards

## 唯一資料庫存取入口

- 只有 `apps/api` 可以 import `@opentrade/db` 的 PrismaClient
- 前端 (`apps/web`、`apps/console`) 只能 `import type`，不可 import client
- Schema 唯一 source of truth：`packages/db/prisma/schema.prisma`

---

## Schema 命名規範

```prisma
model Review {
  id              String   @id @default(uuid()) @db.Uuid
  tenantId        String   @db.Uuid
  brokerId        String   @db.Uuid
  authorUserId    String   @db.Uuid
  sentiment       Sentiment
  body            String   @db.Text
  ipfsCid         String?
  chainTxHash     String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  deletedAt       DateTime?

  broker          Broker   @relation(fields: [brokerId], references: [id])
  author          User     @relation(fields: [authorUserId], references: [id])

  @@index([tenantId, brokerId])
  @@index([tenantId, createdAt])
  @@map("reviews")
}
```

### 規則

- Model 名：PascalCase（`Review`、`KolSignal`）
- 欄位：camelCase
- DB 表名：snake_case（`@@map("reviews")`）
- DB 欄位（hidden）：自動 snake_case（Prisma 預設可不寫 `@map`，但不一致時手動指定）
- ID：UUID v4（`@db.Uuid`）
- 時間：`@default(now())` + `@updatedAt`
- 軟刪除：`deletedAt: DateTime?`
- 索引：明確標 `@@index([...])`，不靠隱式

---

## Multi-tenant Ready（強制）

**所有 user-scoped 表必須有 `tenantId`**，即使 V1 只有一個 tenant（HK）：

```prisma
model Broker {
  id        String @id
  tenantId  String @db.Uuid    // 必有
  // ...
}
```

查詢時必須加 `tenantId` filter：

```ts
// ❌ 不可
prisma.broker.findMany({ where: { name: 'XYZ' } });

// ✅ 應該
prisma.broker.findMany({
  where: {
    tenantId: ctx.tenantId,
    name: 'XYZ',
  },
});
```

最佳做法：用 Prisma extension 自動注入 `tenantId`（Phase 0 後期實作）。

### 例外：全域參考表（無 `tenantId`）

本規則只強制 **user-scoped 表**（評論、KOL、投訴、驗證等屬於某 tenant 用戶的資料）。
**全域市場參考表**（所有 tenant 共用、非任何用戶擁有的 lookup 資料）**不應**有 `tenantId`：

```prisma
model Instrument {
  id        String       @id @default(uuid()) @db.Uuid
  category  AssetClass   // 無 tenantId — 全域市場參考表（per ADR-0038）
  symbol    String
  // ...
  @@unique([category, symbol])
  @@map("instruments")
}
```

判準：資料是否「屬於某個用戶 / 某個 tenant」？
- ✅ 屬於 → 必有 `tenantId`（review、kol、complaint、signal、note…）
- ❌ 不屬於、全域共用的 lookup → **不加** `tenantId`（instrument、未來的幣別 / 國家 / 交易所代碼表）

加全域參考表必須在 ADR 載明此例外（per ADR-0038 §Instrument no tenantId）。

---

## 軟刪除（Soft Delete）

- **絕不真的 DELETE**（除了測試 / GDPR 合規 hard delete）
- 改 `deletedAt` 欄位
- 查詢時自動排除 `deletedAt: null`（用 Prisma extension）

例外：
- `outbox_events` 處理完後可真的 DELETE（保留時間 7 天）
- 測試資料

---

## Migration 規範

### 命名

```
20260518120000_add_review_signal_relation
```

格式：`{timestamp}_{verb}_{noun}_{detail}`

### 流程

1. 改 `schema.prisma`
2. 跑 `pnpm db:migrate dev --name {description}`
3. Review 產生的 SQL，確保沒有破壞性操作
4. 跑測試
5. Commit `schema.prisma` + `migrations/`

### 嚴禁

- ❌ 手改 `migrations/` 內的 SQL（除非 Prisma 產生有 bug，且要 ADR）
- ❌ 在 prod 用 `db:migrate dev`（用 `db:migrate deploy`）
- ❌ 不 review 直接執行 migration
- ❌ Drop column 不分階段（先 deprecate → 兩個 release 後才 drop）

---

## Repository Pattern

`apps/api` 不直接呼叫 Prisma，透過 Repository：

```ts
// domains/reviews/domain/IReviewRepository.ts
export interface IReviewRepository {
  findById(id: ReviewId): Promise<Review | null>;
  save(review: Review): Promise<void>;
  list(filter: ReviewFilter): Promise<Review[]>;
}

// domains/reviews/infrastructure/PrismaReviewRepository.ts
export class PrismaReviewRepository implements IReviewRepository {
  constructor(private readonly prisma: PrismaClient) {}
  // 實作...
}
```

Use case 接受 interface，不接受具體實作：

```ts
class SubmitReviewUseCase {
  constructor(private readonly reviewRepo: IReviewRepository) {}
}
```

---

## Transaction

跨表寫入必須在 transaction 內：

```ts
await prisma.$transaction(async (tx) => {
  await tx.review.create({ data: ... });
  await tx.outboxEvent.create({ data: ... });
});
```

特別重要：**業務寫入 + Outbox event 必須同 transaction**（避免事件丟失）。

---

## 索引策略

每個 query pattern 必須有對應索引：

- 單欄 PK：`@id`
- Foreign key：自動建 index
- 列表查詢：`@@index([tenantId, status, createdAt])`（複合索引，order 重要）
- 唯一限制：`@@unique([...])`

不可用的索引：
- ❌ `@@index([])` 沒實際 query 用到的索引（浪費空間）
- ❌ 只有 `tenantId` 單欄的索引（沒區別性）

---

## 連線管理

- `PrismaClient` 全 app 一個實例（`packages/db/src/client.ts`）
- 不在 hot path 創建新 instance
- 用環境變數 `DATABASE_URL`（從 AWS Secrets Manager 注入）
- Read replica：另一個 client 連 readonly URL

```ts
// packages/db/src/client.ts
export const prisma = new PrismaClient({
  log: ['warn', 'error'],
});

export const prismaReadOnly = new PrismaClient({
  datasources: {
    db: { url: env.DATABASE_READ_URL },
  },
});
```

---

## Seed 資料

Seed 路徑：`packages/db/scripts/seed.ts`，由 `pnpm --filter @opentrade/db db:seed` 觸發。

### 兩類 seed data，規則不同

| 類別                | 例子                                       | 規則                                     |
| ------------------- | ------------------------------------------ | ---------------------------------------- |
| **Bootstrap data**  | `Tenant`、系統角色、預設地區設定           | 可在 `seed.ts` 內 hardcode（單一來源即可） |
| **Domain data**     | SFC 持牌券商清單、KOL 名錄、預載評論       | 必須從外部 JSON / CSV 讀，避免 schema-by-source-edit |

#### 為何 bootstrap data 准 hardcode？

Bootstrap data 滿足三個條件才合格：

1. **基數固定且極小**（V1 只有 1 個 Tenant `hk`，Phase 2 才加 `tw` / `sg`）
2. **schema 級必要**（後續任何 insert 都依賴它的 FK，缺它整個 DB 無法用）
3. **變動需要 ADR / migration 級審視**（不是平常 ops 修改的東西）

Domain data 不滿足任一條，必須走 JSON：可隨資料源更新而不必改 code、可由非 dev 角色維護、可獨立 review。

### 通用規則

- **必須冪等**（同 DB 重跑 seed 結果不變）：用 `prisma.upsert` 加 natural unique key（`Tenant.code`、`Broker.slug + tenantId`）
- **不可在 seed 內 truncate / delete**（避免誤刪 prod）
- **`update` 區段保守**：只刷 bootstrap defaults，不 clobber operator 後來改的欄位（如 `isActive`）

### 範例（bootstrap data，hardcode 合法）

```ts
// packages/db/scripts/seed.ts
const tenants = [
  { code: 'hk', name: 'Hong Kong', defaultLocale: 'zh-Hant', timezone: 'Asia/Hong_Kong' },
] as const;

for (const t of tenants) {
  await prisma.tenant.upsert({
    where: { code: t.code },
    update: { name: t.name, defaultLocale: t.defaultLocale, timezone: t.timezone },
    create: { ...t, isActive: true },
  });
}
```

### 範例（domain data，必須走 JSON）

```ts
// Phase 1：預載 SFC 持牌證券商清單
const brokers = JSON.parse(
  await fs.readFile('packages/db/seed-data/sfc-brokers.json', 'utf-8'),
) satisfies BrokerSeed[];

for (const b of brokers) { /* upsert by slug */ }
```

---

## 外部參考資料同步（Reference Data Sync）

不同於一次性 seed（bootstrap/domain）與一次性 backfill（schema migration 回填），
有第三類資料流程：**可反覆重跑、從外部來源同步進全域參考表**（per ADR-0038 instrument catalog）。

範例：`Instrument` 目錄從 HKEX / SEC / CoinGecko + 策展 JSON 同步
（`packages/db/src/catalog/` + `pnpm --filter @opentrade/db sync:instruments`）。

### 強制結構

1. **Source-agnostic 冪等 upsert**：以 natural key（如 `[category, symbol]`）upsert，重跑結果穩定
2. **來源失敗隔離**：單一外部來源掛掉只 `warn + skip`，不可中斷其他來源
3. **per-source 軟退場對賬**：某來源不再出現的 row 標 `isActive=false`（**絕不 hard-delete**，per §軟刪除）；
   且**只對賬該來源自己的 row**（partial sync 不可誤殺其他來源）
4. **不在 request time 打外部 API**：搜尋 / 查詢一律走本地已同步的表（延遲 + rate limit + 合規考量）
5. **免金鑰來源優先**：能用免金鑰公開資料就不要引入需 key 的來源（降低 secret 管理面）
6. **sync 用依賴不進 runtime bundle**：解析用的重量套件（如 XLSX 解壓）放 `devDependency`，
   只在 offline sync script 用，不進 API / worker bundle

### 嚴禁

- ❌ 把外部參考清單 hardcode 進 schema 或 code（per §Seed domain data 原則）
- ❌ 在 API request path 即時打外部來源 API（必須先同步進本地表）
- ❌ sync 對賬時 hard-delete 退場的 row（用 `isActive` 軟退場）
- ❌ 一個來源失敗就中斷整個 sync（必須來源獨立容錯）

---

## Backfill Scripts（資料遷移）

當 schema 變更需要回填既有資料（per 上方「Migration 規範」嚴禁中「Migration 內含資料遷移」），**必須**寫獨立 backfill script 放在 `packages/db/scripts/`。

### 強制結構（per ADR-0026 lesson + 2026-05-25 M1.2 fix）

每個 backfill script 必須：

1. **Cursor pagination**（**禁用** mutable-WHERE pattern）
   - ❌ 舊 mutable-WHERE pattern（`WHERE field IS NULL` + 「每次都重新 query」）在 LIVE 模式下因為 update 收縮 WHERE 集合而「看起來」work，但在 dry-run 模式下會死迴圈（WHERE 集合永遠不縮）
   - ✅ Cursor pagination（`WHERE id > lastSeenId`）對 LIVE + DRY_RUN 都 universally correct，且 idempotent
2. **`--dry-run` flag 強制支援**
   - 透過 `process.argv.includes('--dry-run')` 偵測
   - DRY_RUN 模式下：跑 classification / transformation 邏輯但**不寫 DB**，console 印「would update」訊息
   - LIVE 模式下：正常 `tx.update`
3. **Idempotent**
   - 第二次跑必須 no-op（透過 WHERE 排除已處理 rows + cursor 嚴格前進）
4. **Wire 進 `package.json`**
   - 個別 script：`"db:backfill:<noun>": "tsx scripts/backfill-<noun>.ts"`
   - Production aggregator：`"db:backfill:prod": "pnpm db:backfill:<a> && pnpm db:backfill:<b>"`
5. **記錄進 `packages/db/README.md` §Pre-deploy backfill**
   - 註明對應的 ADR / migration ID
   - 列出 LIVE + DRY_RUN 兩個範例 command
6. **同步進 `apps/api/README.md` §Production deployment runbook**

### 範本

```ts
// packages/db/scripts/backfill-<noun>.ts
import { prisma } from '../src';

const BATCH_SIZE = 200;
const DRY_RUN = process.argv.includes('--dry-run');

async function main() {
 console.log(`[backfill-<noun>] mode=${DRY_RUN ? 'DRY_RUN' : 'LIVE'}`);

 let lastSeenId: string | null = null;
 let processedCount = 0;
 let updatedCount = 0;

 // eslint-disable-next-line no-constant-condition
 while (true) {
  const batch = await prisma.<model>.findMany({
   where: {
    <fieldToBackfill>: null,
    ...(lastSeenId !== null ? { id: { gt: lastSeenId } } : {}),
   },
   orderBy: { id: 'asc' },
   take: BATCH_SIZE,
   select: { id: true, <sourceFields>: true },
  });

  if (batch.length === 0) break;

  for (const row of batch) {
   const newValue = classifyOrTransform(row);
   processedCount++;
   if (newValue !== null) {
    if (!DRY_RUN) {
     await prisma.<model>.update({ where: { id: row.id }, data: { <fieldToBackfill>: newValue } });
    }
    updatedCount++;
   }
   lastSeenId = row.id; // cursor 嚴格前進，即使 skip / dry-run 也推進
  }

  console.log(`[backfill-<noun>] processed=${processedCount} updated=${updatedCount}${DRY_RUN ? ' (dry-run)' : ''}`);
 }

 console.log(`[backfill-<noun>] done. final processed=${processedCount} updated=${updatedCount}`);
}

main()
 .catch((err) => {
  console.error(err);
  process.exit(1);
 })
 .finally(() => prisma.$disconnect());
```

### 嚴禁

- ❌ Mutable-WHERE pattern（會在 dry-run 死迴圈）
- ❌ 不支援 `--dry-run`（production deploy 前無法 preview）
- ❌ 跳過 `packages/db/README.md` + `apps/api/README.md` 文件更新
- ❌ 在 `migrations/*.sql` 內寫 `UPDATE ... SET ...`（資料遷移走獨立 script）
- ❌ 不 idempotent 的 backfill（第二次跑會壞資料）

---

## 嚴禁

- ❌ 用 raw SQL 繞過 Prisma（除非有充分理由 + ADR）
- ❌ 在 schema 用 `String` 存 enum（用 Prisma `enum` 或在 app 層 zod）
- ❌ 用 Float 存金錢（用 `Decimal` 或整數 cents）
- ❌ N+1 query（用 `include` / `select`）
- ❌ Migration 內含資料遷移（資料遷移用獨立 script，per §Backfill Scripts）
- ❌ Backfill script 用 mutable-WHERE 而非 cursor pagination（per §Backfill Scripts，dry-run 會死迴圈）
- ❌ Backfill script 不支援 `--dry-run`
- ❌ Prisma Studio 在 prod 連線
- ❌ DB 直接從 console.aws.amazon.com 改 schema（必須 Terraform + Prisma migration）
