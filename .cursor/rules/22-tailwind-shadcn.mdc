---
description: Tailwind CSS、shadcn/ui 與設計系統規範
globs: ["**/*.tsx", "**/*.css", "packages/ui/**/*"]
alwaysApply: false
---

# Tailwind & shadcn/ui Standards

## 設計系統 first

UI 開發遵守 ADR-0009 + ADR-0011：所有元件先在 `packages/ui` 建好 + 寫 stories，再被 apps 使用。設計語言：「Civic Trust + Web3 科技感」，雙主色 Sapphire + Gilded。

---

## Design Tokens（單一來源）

所有顏色、字型、間距、陰影只能來自 `packages/ui/src/design-tokens/`。實際結構分兩層：

```ts
// packages/ui/src/design-tokens/colors.ts
export const palette = {
  sapphire: { 50: '214 100% 97%', ..., 950: '226 57% 21%' }, // HSL 三元組
  gilded:   { 50: '...', ..., 950: '...' },
  emerald:  { 50: '...', ..., 950: '...' },  // success
  vermilion:{ 50: '...', ..., 950: '...' },  // danger
  amber:    { 50: '...', ..., 950: '...' },  // warning
  neutral:  { 0:  '0 0% 100%', ..., 950: '225 40% 7%' },
} as const;

export const semantic = {
  light: { background, foreground, primary, accent, ... },
  dark:  { background, foreground, primary, accent, ... },
} as const;
```

### Tailwind preset 共用

Apps 不自行寫 Tailwind theme，必須走 `@opentrade/ui/tailwind-preset`：

```ts
// apps/web/tailwind.config.ts
import preset from '@opentrade/ui/tailwind-preset';
export default {
  presets: [preset],
  content: ['./src/**/*.{ts,tsx}', '../../packages/ui/src/**/*.{ts,tsx}'],
};
```

### Light / Dark 切換

Semantic tokens 同時映射到 `:root` 與 `.dark` 的 CSS 變數（見 `packages/ui/src/styles/globals.css`），切換靠在 `<html>` 上加 `.dark` class（`next-themes` 處理）。

| App | 預設主題 |
|---|---|
| `apps/web` | Light |
| `apps/console` | Dark |
| `packages/ui` Storybook | Light（addon-themes 可切） |

### 嚴禁

- ❌ 在 component 用 `text-[#1a2b3c]`（hard-coded color）
- ❌ 在 component 用 raw palette 如 `bg-sapphire-700`（必須走 semantic，例如 `bg-primary`）
- ❌ 自己定義 design token 在 component 層
- ❌ 在 apps 自寫 `tailwind.config.ts` theme（必須 extends `tailwind.preset.ts`）
- ❌ Gilded accent 濫用作裝飾色（≤ 10% 畫面，限 SBT / verified / premium 狀態，見 ADR-0011）

---

## 字型

由 `packages/ui/design-tokens/typography.ts` 定義：

- 繁中 / 簡中標題：思源宋體
- 繁中 / 簡中內文：思源黑體
- 英文 / 數字：Inter
- 等寬：JetBrains Mono

字型透過 `next/font/local` 自託管（GDPR / 隱私考量）。

---

## shadcn/ui 使用方式

- 透過 shadcn CLI 加入元件到 `packages/ui/primitives/`
- 不直接使用 raw shadcn — 必須在 `packages/ui` 包一層 OpenTrade 風格的 wrapper
- 客製化只能改 token / variant，不改 API surface

---

## Class Name 規範

### 順序（ESLint plugin 強制）

1. Layout: `flex grid block hidden`
2. Position: `relative absolute`
3. Display: `w-* h-* p-* m-*`
4. Typography: `text-* font-*`
5. Color: `bg-* text-*`
6. Border: `border-* rounded-*`
7. Effects: `shadow-* opacity-*`
8. Animation: `transition-* hover:* focus:*`

### 條件 class

用 `cn()` helper（位於 `@opentrade/ui/utils`，內部組合 `clsx` + `tailwind-merge`）：

```tsx
import { cn } from '@opentrade/ui/utils';

<button className={cn(
  'rounded px-4 py-2',
  variant === 'primary' && 'bg-primary text-primary-foreground',
  disabled && 'opacity-50 cursor-not-allowed',
)} />
```

複雜 variant 用 `class-variance-authority` (cva) — 參考 `packages/ui/src/primitives/button/Button.tsx`。

### 嚴禁

- ❌ Inline style（除非 dynamic value 無法用 class，且必須 ADR）
- ❌ `!important` (`!text-red-500`)，除非有充分理由註解
- ❌ 跨多個元件複製貼相同 class string（要包成 component）

---

## 響應式設計

- **apps/web**：mobile-first
  - 不加前綴 = 手機樣式
  - `md:` 平板以上
  - `lg:` 桌面
- **apps/console**：desktop-first
  - 預設桌面樣式
  - `max-md:` 才考慮平板 / 手機（功能精簡版）

---

## 暗色模式

- `packages/ui` 設計系統支援 light / dark
- 用 `dark:` 前綴
- 切換邏輯由 `next-themes` 處理
- 預設跟隨系統

---

## 動畫

- 用 Tailwind 的 `transition-*`
- 複雜動畫用 `framer-motion`
- 動畫時長走 design token：
  - `motion-fast` = 150ms
  - `motion-base` = 250ms
  - `motion-slow` = 400ms

避免：
- ❌ 不必要的閃爍動畫
- ❌ 違反 `prefers-reduced-motion`

---

## 元件變體（variant）

用 `class-variance-authority` (`cva`)：

```ts
const buttonVariants = cva(
  'inline-flex items-center rounded-md',
  {
    variants: {
      intent: {
        primary: 'bg-primary-500 text-white',
        secondary: 'bg-neutral-200 text-neutral-900',
        danger: 'bg-danger-500 text-white',
      },
      size: {
        sm: 'px-2 py-1 text-sm',
        md: 'px-4 py-2',
        lg: 'px-6 py-3 text-lg',
      },
    },
    defaultVariants: { intent: 'primary', size: 'md' },
  },
);
```

---

## Storybook 必須

每個 `packages/ui/src/{primitives,compounds}/<name>/` 元件必須有：
- `<Name>.tsx`
- `<Name>.stories.tsx`（至少 3 個 stories：default、edge case、所有 variants）
- 必要時 `<Name>.test.tsx`

stories 必須測試：
- 不同語言（zh-Hant / zh-Hans / en）
- 不同主題（light / dark）— Storybook 工具列切換
- Loading / empty / error 狀態
- 不同裝置 viewport（已預設 xs / sm / md / lg / xl）
- 至少一個 **in-context demo**（元件在真實使用情境下，例如 Button 在表單內、ImmutableMark 在 ReviewCard 內）

參考實作：`packages/ui/src/primitives/button/Button.stories.tsx`、`packages/ui/src/compounds/immutable-mark/ImmutableMark.stories.tsx`。

---

## OpenTrade 視覺武器（ADR-0011 §5）

這些是品牌記憶點，**不可隨意改動或弱化**：

- `<ImmutableMark>` — 每筆鏈上資料（評論、KOL 訊號、仲裁結果）的章戳。**只用於真正上鏈的資料**，不可用在 API / 商戶聲明上（會誤導使用者）。
- `<SBTBadge>` — L1/L2/L3/L4 用戶等級徽章（Phase 1 實作）
- 自製 justice / jury / verdict icon set（Phase 0.6 實作）

新增業務 compound 時，先檢查是否該使用 ImmutableMark 或 SBTBadge — 維持視覺一致性是品牌價值的一部分。

---

## 嚴禁

- ❌ 在 `apps/web` / `apps/console` inline 寫元件並期望「之後重構到 ui」
- ❌ 重複實作已存在於 `packages/ui` 的元件
- ❌ 改 `packages/ui` 元件 API 卻不更新所有使用處
- ❌ 把業務邏輯 / API 呼叫寫進 `packages/ui` 元件（必須純展示）
- ❌ Tailwind classes 一行超過 100 字（拆 cn 或 cva）
- ❌ ImmutableMark 顯示在非鏈上資料（違反 ADR-0011 §5.1）
- ❌ 在 `apps/*` 自寫 Tailwind theme 而不 extends `tailwind.preset.ts`
