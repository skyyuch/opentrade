---
description: Web3 前端整合（wagmi + viem + Privy）規範
globs: ["apps/web/**/*", "apps/console/**/*", "packages/ui/**/*"]
alwaysApply: false
---

# Web3 Frontend Standards

## 技術棧

- **wagmi v2**（不是 v1）
- **viem**（不用 ethers.js）
- **Privy**（社交登入 + Account Abstraction）

詳見 ADR-0005。

---

## Provider 設定

頁面 root 必須包：

```tsx
// apps/web/src/app/[locale]/providers.tsx
'use client';

import { PrivyProvider } from '@privy-io/react-auth';
import { WagmiProvider } from '@privy-io/wagmi';
import { wagmiConfig } from '@opentrade/config/wagmi';

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <PrivyProvider
      appId={env.NEXT_PUBLIC_PRIVY_APP_ID}
      config={privyConfig}
    >
      <WagmiProvider config={wagmiConfig}>
        <QueryClientProvider client={queryClient}>
          {children}
        </QueryClientProvider>
      </WagmiProvider>
    </PrivyProvider>
  );
}
```

---

## 鏈設定（多鏈 ready）

**絕不寫死 Base**。從 `@opentrade/config` 讀取：

```ts
// packages/config/src/chains.ts
import { base, baseSepolia } from 'viem/chains';

export const targetChain = env.CHAIN === 'mainnet' ? base : baseSepolia;
export const supportedChains = [base, baseSepolia] as const;
```

---

## 與 API 整合

OpenTrade 使用兩步驟 auth flow（per rule 50 + ADR-0005）：

1. 前端用 Privy access token 換取 OpenTrade JWT：`POST /v1/auth/exchange`
2. 後續 API 呼叫用 OpenTrade JWT（ES256）

**iAM Smart 第三條路徑**（per ADR-0031 D7）：

iAM Smart 不走 Privy native flow。前端 render 自訂「Login with iAM Smart」按鈕，觸發 `POST /v1/auth/iam-smart/start` 取得 OGCIO redirect URL，用戶完成後 API callback 直接簽發 OpenTrade JWT（跳過 Privy token exchange 步驟）。JWT 結構與 Privy path 完全相同，下游 middleware 不需區分來源。

---

## 登入入口（Phase 2 UI Polish S2 後）

**絕不直接呼叫 `usePrivy().login()` 開預設 modal**。OpenTrade 走 **full-page `/auth` 路由 + branded UI**（見 `apps/web/src/app/[locale]/auth/page.tsx`），用 Privy headless hooks 渲染 5 種登入方式：

- **Wallet** → `useConnectWallet().connectWallet()`（仍會 open Privy 的 wallet picker，是 wagmi 抽象的必要層）
- **iAM Smart** → 點按鈕 open info modal（per ADR-0031 D7 過渡期 — 不接後端，待 OGCIO sandbox 授權後升級）
- **Google** → `useLoginWithOAuth().initOAuth({ provider: 'google' })`（redirect flow）
- **Apple** → `useLoginWithOAuth().initOAuth({ provider: 'apple' })`（redirect flow）
- **Email** → `useLoginWithEmail()` 兩 step（`sendCode` → `loginWithCode`）

新 / 既有 callsite **必須**用 `useLoginRedirect()` helper，**禁止**重新引入 `const { login } = usePrivy()` 的舊 pattern：

```tsx
import { useLoginRedirect } from '@/hooks/useLoginRedirect';

const goLogin = useLoginRedirect();
// onClick={goLogin}  → router.push(`/auth?returnUrl=<currentPathWithSearch>`)
```

helper 已內建：

- 自動把當前 locale-aware pathname + searchParams 編進 `returnUrl`
- 走 `next-intl/navigation` 的 locale-aware router，return path 自動帶回正確 locale
- 跳過 `/auth → /auth` 迴圈（已在 /auth 不重複 push）

`/auth` 頁本身額外負責：

- **Open-redirect 防護**：`returnUrl` 只接受 same-origin root-relative path（必須以單一 `/` 開頭、不可 `//` / `/\\`），不通過 allow-list 就 fall back 到 `/`
- **OAuth roundtrip**：`initOAuth` 走 `window.location.assign(...)` 會丟失 in-memory state，所以驗證過的 `returnUrl` 先寫入 `sessionStorage`（`opentrade.auth.returnUrl`），OAuth callback 回 `/auth` 後 post-auth `useEffect` 讀回再 `router.replace(target)`
- **OpenTrade JWT exchange**：`/auth` 頁**不**負責 exchange — 各既有頁面（透過 `useOpenTradeAuth`）登入後自己 reactive 觸發，`/auth` 只 own Privy 一步

```ts
import { usePrivy } from '@privy-io/react-auth';

function useApi() {
  const { getAccessToken } = usePrivy();
  const [otJwt, setOtJwt] = useState<string | null>(null);

  async function exchangeToken() {
    const privyToken = await getAccessToken();
    const res = await fetch(`${env.NEXT_PUBLIC_API_URL}/v1/auth/exchange`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ privyAccessToken: privyToken }),
    });
    const { data } = await res.json();
    setOtJwt(data.accessToken);
    return data.accessToken;
  }

  return async function api<T>(path: string, init?: RequestInit) {
    const token = otJwt ?? await exchangeToken();
    const res = await fetch(`${env.NEXT_PUBLIC_API_URL}${path}`, {
      ...init,
      headers: {
        ...init?.headers,
        'Authorization': `Bearer ${token}`,
        'Content-Type': 'application/json',
      },
    });
    if (!res.ok) throw new ApiError(...);
    return res.json() as Promise<T>;
  };
}
```

---

## 合約呼叫

### 讀（read）

```ts
import { useReadContract } from 'wagmi';
import { reviewRegistryAbi } from '@opentrade/contracts/abi';

const { data, isLoading } = useReadContract({
  address: contractAddresses.reviewRegistry,
  abi: reviewRegistryAbi,
  functionName: 'getReview',
  args: [reviewId],
});
```

### 寫（write）

```ts
import { useWriteContract } from 'wagmi';

const { writeContract, isPending } = useWriteContract();

writeContract({
  address: contractAddresses.reviewRegistry,
  abi: reviewRegistryAbi,
  functionName: 'submitReview',
  args: [brokerId, contentHash, ipfsCid],
});
```

### 等待 receipt

```ts
import { useWaitForTransactionReceipt } from 'wagmi';

const { data: receipt } = useWaitForTransactionReceipt({ hash });
```

---

## 合約地址管理

統一從 `@opentrade/config/contracts` 讀：

```ts
// packages/config/src/contracts.ts
export const contractAddresses = {
  reviewRegistry: env.NEXT_PUBLIC_REVIEW_REGISTRY_ADDRESS as Address,
  juryPool: env.NEXT_PUBLIC_JURY_POOL_ADDRESS as Address,
  signalLogger: env.NEXT_PUBLIC_SIGNAL_LOGGER_ADDRESS as Address,
};
```

絕不寫死合約地址在 component。

---

## ABI 來源

ABI 從 `@opentrade/contracts` 包匯出（由 Foundry 編譯後產生）：

```ts
// packages/contracts/abi/index.ts
export { reviewRegistryAbi } from './ReviewRegistry.abi';
// ...
```

---

## Gasless / Paymaster

OpenTrade 大部分使用者操作必須 gasless：

```ts
// 透過 Privy 的 paymaster 功能
const txHash = await sendTransactionWithPaymaster({
  to: contractAddresses.reviewRegistry,
  data: encodeFunctionData({ ... }),
});
```

不允許直接讓使用者付 gas（除非該操作明確指定使用者付，例如 KOL 發訊號）。

---

## 錢包顯示

地址必須脫敏顯示：

```tsx
import { shortenAddress } from '@opentrade/shared';

<span>{shortenAddress(address)}</span>
// 0x1234...abcd
```

提供「複製完整地址」按鈕。

---

## 鏈上資料快取

- Read 結果用 wagmi 的內建 cache（透過 TanStack Query）
- 寫入後 invalidate 相關 query
- 不可在 component 自己存「上次合約讀到什麼」

---

## 鏈上事件監聽

前端不直接監聽 contract events（成本高、不穩）。改為：

1. 後端 worker 監聽 events → 寫進 DB
2. 前端 poll API（或 SSE）
3. 顯示更新

---

## 顯示 tx 狀態

從「使用者點按鈕」到「上鏈成功」流程：

```
1. 使用者點「提交」
2. 顯示 modal: "Confirming with wallet..."
3. Privy 顯示確認 UI（社交登入用戶會跳 Privy 內建確認）
4. 拿到 hash → 顯示 "Waiting for confirmation on Base..."
5. 1 個區塊確認後 → 顯示 "Submitted! 🎉"
6. 提供 link to BaseScan
```

每一步都有 i18n 文案（不可寫死英文）。

---

## 嚴禁

- ❌ 直接 import ethers.js（用 viem）
- ❌ 寫死 chainId / 合約地址 / RPC URL
- ❌ 把 Privy server secret 暴露到前端
- ❌ 把 wallet 私鑰存 localStorage / sessionStorage
- ❌ 跳過 wagmi 直接 `new ethers.Contract(...)`（失去 cache / type safety）
- ❌ 在 SSR 期間呼叫 wallet 相關 hook（必 client-side only）
- ❌ 在 production 用 testnet RPC
- ❌ 用 `as` 強制斷言 address（用 `Address` type from viem）
- ❌ 直接呼叫 `usePrivy().login()` 開預設 modal（走 `/auth` 全頁路由 + `useLoginRedirect()` helper — 見「登入入口」段）
- ❌ 在 `returnUrl` 接收任何非 same-origin / 非 root-relative 的 path（open-redirect risk — `/auth` 頁的 `isSafeReturnUrl()` 已 enforce，callsite 不可繞過）
