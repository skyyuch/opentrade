{
  "name": "@opentrade/api",
  "version": "0.0.0",
  "private": true,
  "description": "OpenTrade backend API. Hono on Node.js with DDD + Modular Monolith architecture.",
  "type": "module",
  "scripts": {
    "build": "echo 'tsup not yet initialized — see Phase 0 commit #3' && exit 0",
    "dev": "echo 'dev not yet initialized' && exit 0",
    "start": "echo 'start not yet initialized' && exit 0",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet' && exit 0",
    "test:unit": "echo 'no tests yet' && exit 0",
    "clean": "rm -rf .turbo dist node_modules/.cache"
  },
  "dependencies": {
    "@opentrade/config": "workspace:*",
    "@opentrade/db": "workspace:*",
    "@opentrade/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
